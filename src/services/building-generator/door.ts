export class Door {
    private width: number;
    private height: number;
    private left: number
    constructor(width: number, height: number) {
        this.width = width;
        this.height = height;
        this.left = 0;
    }
}